# This pipeline uses only Python's standard library (pathlib, json, datetime, ast)
# No third-party dependencies are required to run it.
#
# DuckDB is used separately as a CLI tool to query the pipeline's output,
# and is not a Python dependency of this codebase.
# Install it directly: https://duckdb.org/docs/installation